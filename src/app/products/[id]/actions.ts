"use server";

export async function incrementProductQuantity(productId: string) {
  // This is a mock implementation since we removed authentication
  console.log(`Adding product ${productId} to cart`);
}